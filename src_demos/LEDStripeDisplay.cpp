#include "LEDStripeDisplay.h"
#include <iostream>

void LEDStripeDisplay::setPercentage(unsigned long percentage) {
    std::cout << "LED Stripe Display set to " << percentage << "%" << std::endl;
    // Add code to control the LED stripe display accordingly
}
